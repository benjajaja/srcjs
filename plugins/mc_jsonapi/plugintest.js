var plugin = require('./plugin');var eventBus = require('../../srcjs/eventbus')();var io = {	of: function(name) {		var io = require('../../srcjs/eventbus')();				return io;	}};exports.testSocketIO = function(test) {		plugin = plugin(eventBus, io, "pluginname", {});		var socket = require('../../srcjs/eventbus')();	io.of().emit('connection', socket);	socket.emit('command', {command: 'test', args: []}, function(err) {		console.log('command: '+err);	});		test.done();};